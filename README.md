# lucas-hire-page

Solo hire page for Lucas Bento — DR copy + ads video editing.

## Local

Open `index.html` in a browser, or:

```bash
python3 -m http.server 8080
```

Visit `http://localhost:8080`.

## Host (GitHub Pages)

1. Repo **Settings → Pages**
2. **Build and deployment → Source**: Deploy from a branch
3. Branch: `main` · Folder: `/ (root)`
4. Save. URL: `https://lucaspbdesign-source.github.io/lucas-hire-page/`

## Before you share widely

- Replace the mailto on the page: currently `hello@lucasbento.com` (placeholder — confirm the real address).
- Swap proof metrics and case-study results labeled “example”.
